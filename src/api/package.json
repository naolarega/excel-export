{
  "name": "api",
  "version": "1.0.0",
  "description": "API backend for excel export",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {}
}
